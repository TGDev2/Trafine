import React from "react";
import { View } from "react-native";

/**
 * Placeholder screen to register the Logout tab in the bottom bar.
 * Tab press is intercepted in _layout.tsx to show confirmation.
 */
export default function LogoutPlaceholder() {
  return <View />;
}
